import { NgModule } from '@angular/core';
import { JsonIteratePipe } from './json-iterate/json-iterate';
@NgModule({
	declarations: [JsonIteratePipe],
	imports: [],
	exports: [JsonIteratePipe]
})
export class PipesModule {}
